from setuptools import setup, find_packages

setup(
    name="daytona",
    version="0.0.1",
    description="Stub package for Daytona SDK — satisfies imports without real SDK",
    packages=find_packages(),
    python_requires=">=3.10",
)
